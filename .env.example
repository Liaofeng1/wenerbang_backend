PORT=8080
APP_ENV=dev
JWT_SECRET=dev-secret-change-in-production
# SQLite file for hackathon demo
DATABASE_DSN=file:wenbang.db?cache=shared&mode=rwc
REGISTER_BONUS=30
INVITE_REWARD=50
# Fixed publish cost (plus any bounty freeze)
PUBLISH_COST=5
# Fallback Tmin if a survey did not set one
MIN_AWAY_SECONDS=120
